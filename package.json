{
  "scripts": {
    "build": "./node_modules/.bin/babel src -d dist -s --copy-files",
    "prepublish": "npm run build",
    "buildwatch": "./node_modules/.bin/babel -w src -d dist -s --copy-files",
    "test": "babel-node dist/spec/run.js"
  },
  "files": [
    "dist"
  ],
  "name": "emoji-cipher-lg-mc",
  "version": "1.1.0",
  "description": "Learners Guild challenge #7",
  "main": "dist/emoji-cipher.js",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "jasmine": "^2.4.1"
  },
  "author": "Mihai Banulescu, Rachel Ralston, John Hopkins",
  "license": "ISC",
  "dependencies": {
    "node-emoji": "^1.3.1"
  }
}
